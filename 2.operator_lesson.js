//  Operators Practice Page

// Comparision Operators -
//      < less than
//      > greater than
//      <= equal or less than
//      >= equal or greater than
//      === equal to
//      !== not equal to

// Logical Operators and Rules
// && (AND)
//      true && true === true;
//      true && false === false;
//      false && true === false;
//      false && false === false;

// || (OR)
//      true || true === true;
//      true || false === true;
//      false || true === true;
//      false || false === false;

// ! (NOT)
//      !true === false;
//      !false === true;
